# The second def is better because the first returns a value and has a side effect.
# The first def passes in an argument that gets mutated, the second does not mutate its argument.
# The first def returns a reference to the object passed in the argument, the second returns a new object 
